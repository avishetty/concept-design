import React from 'react';
import ReactDOM from 'react-dom/client';
import '../tokens.css';
import { OnboardingApp } from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OnboardingApp/>
  </React.StrictMode>
);
